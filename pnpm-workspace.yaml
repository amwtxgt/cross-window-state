packages:
  # explicit "." — changesets (@manypkg) does not implicitly include the
  # root package when resolving the workspace
  - .
  - examples/*

allowBuilds:
  electron: true
  esbuild: true

minimumReleaseAgeExclude:
  - "@types/node@26.3.0"
  - electron@44.0.0
